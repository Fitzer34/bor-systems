services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: bor
      POSTGRES_PASSWORD: bor
      POSTGRES_DB: bor_systems
    ports:
      - "5432:5432"
    volumes:
      - bor_pg:/var/lib/postgresql/data
volumes:
  bor_pg:

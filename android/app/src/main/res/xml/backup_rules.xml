<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Don't back up the auth token or any local cache to Google's servers.
         allowBackup=false in the manifest already covers this, but the file
         is referenced from the manifest and must exist. -->
</full-backup-content>
